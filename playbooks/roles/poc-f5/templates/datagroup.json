{
  "description": "Generated by CMP",
  "records": [
    {%- for url in var_new_urls -%}
    {
      "name": "{{ url }}",
      "data": ""
    }{%- if not loop.last -%},{% endif %}
    {%- endfor -%}
  ],
  "type": "string",
  "name": "dg_sslo_cat_{{ extra_subscription_name | lower }}"
}